# dashboard

The local interactive dashboard for MIScope analysis. Built on Dash.

A workspace member of this repo's uv workspace; consumes `miscope` as a sibling
package. Run from the repo root: `uv run python -m dashboard.app`.
